function FindProxyForURL(url, host){return "<% if (proxy.rules && proxy.rules.length > 0) {    proxy.rules.forEach(function(rule){
        if (rule.isEnabled){ %>PROXY <%- rule.proxyPath %>;<%}}) } %>DIRECT;";}